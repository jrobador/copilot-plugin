Show the final output of a finished Copilot job by calling the `copilot_result`
MCP tool with the workspace path (and a `job_id` if the user named one; otherwise
the latest finished job). Present it verbatim, preserving findings, file paths
and line numbers.
